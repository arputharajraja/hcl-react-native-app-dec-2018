// BDD - Behaviour Driven Development

describe ("Math Suite", () => {
    it("should add 2+3 equals 5", () => {
        //assertion
        expect(2 + 3).toBe(5)
    })

    it("should add 2-3 equals -1", () => {
        //assertion
        expect(2 - 3).toBe(-1)
    })
});